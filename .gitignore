# ---- large / regenerable artifacts (NOT committed) ----
trained_models/        # ~1 GB RLlib teacher checkpoints; regenerated by auto_loop.py / pipelines
d4/                    # distillation rollout buffers (regenerated by `distill.py collect`)
d4x/
d4x4/
distill_data/
ray_results/
_env_overrides.json    # transient per-run env override written by scripts

# ---- python / os cruft ----
__pycache__/
*.py[cod]
.DS_Store
.ipynb_checkpoints/
*.ipynb

# ---- per-episode metric dumps (the aggregated behavior_metrics_summary.csv IS committed) ----
metrics_per_episode_*.csv
ICRA_matrix_table.txt   # regenerated by make_icra_figure.py alongside the PNG
*.bak
*.log

# ---- latex build products (ICRA_draft.pdf IS committed) ----
paper/*.aux
paper/*.out
paper/*.log
paper/*.synctex.gz
